# Log Server Configuration

# Server port (default: 8081)
PORT=8081

# Buffer capacity - number of log entries to store (default: 1000)
CAPACITY=1000

# Verbose mode - show file/line metadata (default: false)
# Set to 1 or true to enable
VERBOSE=false
